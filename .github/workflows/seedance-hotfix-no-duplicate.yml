name: Retired Seedance Hotfix Guard

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Retired
        run: echo "This July one-off self-modifying hotfix is retired. Davis Video A uses explicit paid-safety modules and must not auto-patch app.js."
